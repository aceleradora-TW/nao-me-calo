<div class='row'>
  <!-- Linha abaixo removivel sem problemas :D -->
  <h1 class='teste'><%= image_tag(@spot.icon, size: "20x20") %> <%= @spot.name %></h1>
</div>
</div class='row'>
  <h5 class='text-center'>Endereço: <%= @spot.formatted_address %></h3>
</div>


<%= render 'form' %>

<%= link_to 'Back', ratings_path %>
